# Fixture: mcp-server importing from a service — VIOLATION (IMP001).
# Owner classification: ("mcp-server", "clawhip-bridge") — see _meta.py
from registry_api import __version__  # mcp-server → service import
