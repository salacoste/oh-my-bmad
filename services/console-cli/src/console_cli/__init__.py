"""console-cli — Local console CLI (Typer) for operator commands. Surface parity with Telegram per FR12.

Story 1.2 ships only `__version__`. Real logic arrives in: Stories 4.1–4.6 (Typer binary, parity commands).
"""

__version__ = "0.1.0"
