"""telegram-gateway — Telegram bot ingress: aiogram v3 dispatcher + webhook + allowlist middleware + command surface.

Story 1.2 ships only `__version__`. Real logic arrives in: Stories 3.1–3.20 (aiogram bootstrap → all command handlers → message templates).
"""

__version__ = "0.1.0"
