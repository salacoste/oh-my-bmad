"""telegram-gateway — Telegram bot ingress (aiogram v3) + outbound message-template routing.

Story 1.2 ships only `__version__`. Real logic arrives in: Stories 3.1–3.20 (aiogram bootstrap → all command handlers → templates).
"""

__version__ = "0.1.0"
