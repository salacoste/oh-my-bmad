"""telegram-gateway aiogram message/callback handlers.

Empty in Story 3.1 — real handlers arrive in Stories 3.3 (`/task`),
3.4 (`/approve`), 3.5 (`/ping`), and 3.14–3.19 (status / logs / stop /
reject / retry / agent).
"""
