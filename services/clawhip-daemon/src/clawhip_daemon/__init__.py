"""clawhip-daemon — clawhip event-bus subprocess supervision + Telegram-sink orchestration.

Story 1.2 ships only `__version__`. Real logic arrives in: Story 7.8 (proactive self-recovered summary in telegram-sink); larger build-out via clawhip vendoring (Story 1.3).
"""

__version__ = "0.1.0"
