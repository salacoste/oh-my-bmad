[project]
name = "clawhip-daemon"
version = "0.1.0"
description = "clawhip event-bus subprocess supervision + Telegram-sink orchestration."
authors = [
    { name = "R2d2", email = "bad.vano23ru@gmail.com" },
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.0,<0.12"]
build-backend = "uv_build"
