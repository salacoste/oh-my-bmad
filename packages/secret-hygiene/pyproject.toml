[project]
name = "secret-hygiene"
version = "0.1.0"
description = "Secret scanner (pre-commit) + structlog log-sanitizer + license-scan wrapper. Three-layer secret-hygiene enforcement per NFR-S1."
authors = [
    { name = "R2d2", email = "bad.vano23ru@gmail.com" },
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.0,<1.0"]
build-backend = "uv_build"
