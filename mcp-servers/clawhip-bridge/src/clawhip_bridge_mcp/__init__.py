"""clawhip-bridge — MCP server: append-only event-emission surface (emit_event, emit_blocker, emit_summary, emit_approval_request, emit_completion). Sole mutation path to registry per FR26.

Story 1.2 ships only `__version__`. Real logic arrives in: Story 2.8 (clawhip-bridge MCP server).
"""

__version__ = "0.1.0"
