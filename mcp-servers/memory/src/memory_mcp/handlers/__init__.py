"""memory-mcp MCP tool handlers (Epic 18).

Stories 18.1 / 18.2 scaffold: ``tools.py`` declares an empty ``TIER_MAP`` and the
``validate_caller_trace_id`` helper. The memory tools themselves
(``memory.read`` / ``memory.search`` / ``memory.write``) land in Stories
18.3 / 18.4.
"""
