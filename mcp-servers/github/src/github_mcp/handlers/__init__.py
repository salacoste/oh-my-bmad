"""github-mcp MCP tool handlers (Epic 16).

Story 16.2 scaffold: ``tools.py`` declares an empty ``TIER_MAP`` and the
``validate_caller_trace_id`` helper. The github tools themselves land in
Stories 16.3 (read, Tier-1) / 16.4 (write, Tier-3).
"""
