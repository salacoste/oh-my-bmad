"""artifact-mcp MCP tool handlers (Epic 19).

Stories 19.1 / 19.2 scaffold: ``tools.py`` declares an empty ``TIER_MAP`` and the
``validate_caller_trace_id`` helper. The artifact tools themselves
(``artifact.get`` / ``artifact.list`` / ``artifact.put`` / ``artifact.delete``)
land in Stories 19.3 / 19.4.
"""
