# Story 8.2: SLSA L2 build-provenance attestation

Status: review

<!-- Created by bmad-create-story 2026-05-15 for Phase 2 Epic 8 (γ Supply-chain hardening). -->
<!-- Companion stories: 8.1 (SBOM) shipped at cc16996; 8.3 (cosign sign) and 8.4 (attach SBOM) follow. -->

## Story

As **R2d2 (operator + sole maintainer)**,
I want **every Platform-published Docker image to carry a SLSA Build Level 2 provenance attestation**,
so that **the operator's deploy-side `cosign verify-attestation --type slsaprovenance` can prove an image was built by the canonical `release.yml` workflow on a specific commit SHA — closing the second of three supply-chain triumvirate gates (cosign signature in Story 8.3, SBOM attestation in Story 8.4)**.

## Acceptance Criteria

1. **(AC-1)** Every image published by `release.yml` (1 base + 7 services = 8 images per release) carries a SLSA L2 provenance attestation pushed to the OCI registry alongside the image, generated by `actions/attest-build-provenance` against the image's content digest.
2. **(AC-2)** `cosign verify-attestation --type slsaprovenance --certificate-identity-regexp "^https://github.com/${OWNER}/oh-my-bmad/\.github/workflows/release\.yml@refs/tags/v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9.-]+)?$" --certificate-oidc-issuer "https://token.actions.githubusercontent.com" <image>@<digest>` succeeds for every released image; the attestation traces back to the specific commit SHA + workflow run ID that built it. **Code-review pass-2 F1+F3**: regexp anchored on BOTH ends (`^...$`) AND matches only semver tags — prevents fork-attestation spoofing AND suffix-injection where an attacker exploits unanchored trailing `.*`.
3. **(AC-3)** The workflow grants the minimal permissions required: `permissions: id-token: write` (OIDC, for keyless attestation) and `permissions: attestations: write` (for `push-to-registry: true`). The existing `contents: read` + `packages: write` permissions are unchanged.
4. **(AC-4)** Third-party action SHA-pinned to a canonical release: `actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32  # v4.1.0` (released 2026-02-26). Follows the existing release.yml convention (`<owner>/<repo>@<40-char-sha>  # vX.Y`).
5. **(AC-5)** If the attestation step fails for any service, that service's matrix entry fails (consistent with `fail-fast: false` matrix isolation from Story 8.1).
6. **(AC-6)** Workflow YAML lint passes (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`) and `actionlint`/`yamllint` produces no errors on the modified file.
7. **(AC-7)** Implementation artifact (`_bmad-output/implementation-artifacts/8-2-slsa-l2-provenance-attestation.md`) updated post-implementation with scope-as-shipped, code-touch diff, and AC checklist; sprint-status.yaml entry transitions `ready-for-dev` → `in-progress` → `review` → `done`.

## Tasks / Subtasks

- [x] **Task 1: Wire `actions/attest-build-provenance` into base-image job** (AC: 1, 3, 4, 5)
  - [x] Added new step in `build-and-push-base` after `Tag base as :latest`, before SBOM steps, named `Generate SLSA L2 provenance attestation (base)`.
  - [x] Pinned to `actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32  # v4.1.0`.
  - [x] Inputs: `subject-name: ${{ env.REGISTRY }}/${{ env.OWNER }}/oh-my-bmad-base`, `subject-digest: ${{ steps.build.outputs.digest }}`, `push-to-registry: true`.
  - [x] `steps.build.outputs.digest` already produced by the existing `docker/build-push-action` step (`id: build`) — no extra config required.

- [x] **Task 2: Wire `actions/attest-build-provenance` into service-matrix job** (AC: 1, 3, 4, 5)
  - [x] Added new step in `build-and-push-services` after `Tag <service> as :latest`, before SBOM steps, named `Generate SLSA L2 provenance attestation (${{ matrix.service }})`.
  - [x] Same SHA pin as Task 1.
  - [x] Inputs: `subject-name: ${{ env.REGISTRY }}/${{ env.OWNER }}/oh-my-bmad-${{ matrix.service }}`, `subject-digest: ${{ steps.build.outputs.digest }}`, `push-to-registry: true`.
  - [x] Per-service `steps.build.outputs.digest` works inside the matrix (each matrix run has its own `build` step scope; verified by YAML lint passing).

- [x] **Task 3: Update workflow `permissions:` block** (AC: 3)
  - [x] Added `id-token: write` (keyless OIDC for `attest-build-provenance` Sigstore cert minting; also unlocks Story 8.3 cosign keyless sign).
  - [x] Added `attestations: write` (required for `push-to-registry: true` to write the attestation back to GHCR via the OCI registry API; also unlocks Story 8.4 cosign attest).
  - [x] `contents: read` + `packages: write` unchanged.

- [x] **Task 4: Verify YAML + smoke** (AC: 6)
  - [x] `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` → `✓ release.yml parses cleanly`.
  - [x] Manual diff review confirms no syntax errors.
  - [x] Grep verification: `grep -c "actions/attest-build-provenance@a2bbfa25..." .github/workflows/release.yml` → `2`.
  - [x] Step-name presence: `grep -c "Generate SLSA L2 provenance attestation" .github/workflows/release.yml` → `2`.
  - [x] Permissions verification: `grep -E "id-token: write|attestations: write" .github/workflows/release.yml` → both present under top-level `permissions:`.

- [x] **Task 5: Update sprint-status + implementation artifact** (AC: 7)
  - [x] `sprint-status.yaml`: `8-2-slsa-l2-provenance-attestation` transitioned `ready-for-dev` → `in-progress` → `review` (this commit).
  - [x] Story file updated: Status `ready-for-dev` → `review`; all task checkboxes `[x]`; Dev Agent Record populated; File List complete; Change Log entry added.
  - [ ] Real release verification deferred — workflow triggers only on `push: tags: ['v*']`. Story closure to `done` happens when operator tags the next Phase 2 release; `verified_via:` will be populated then.

## Dev Notes

### Why SLSA L2, not L1 or L3+

- **SLSA L1** (build provenance exists) is too weak — `docker buildx` already produces unsigned provenance metadata; doesn't prove who built the image.
- **SLSA L2** (signed provenance from a hosted build platform) is what `actions/attest-build-provenance` produces: provenance signed by GitHub's keyless Sigstore identity, traceable to a specific workflow run + commit SHA. **Right tier for the project's threat model** (solo operator, public GHCR, hostile-mirror substitution as the primary attack).
- **SLSA L3+** would require hermetic, parameterless, isolated builds — beyond `actions/attest-build-provenance`'s scope and out-of-budget for Phase 2. Deferred to Phase 4+ if operational scope expands (per ADR-0008).

### Why `actions/attest-build-provenance`, not `slsa-framework/slsa-github-generator`

- `actions/attest-build-provenance` is **GitHub-first-party**, maintained by the GitHub Actions team. Same OIDC chain as cosign keyless (Story 8.3) → one trust root, two attestation types.
- `slsa-framework/slsa-github-generator` is community-maintained, more configurable (supports SLSA L3 patterns with worker-orchestration), but adds a second supply-chain trust root (the slsa-framework team's signing infrastructure). Increases trust-perimeter for marginal gain at L2.
- **Decision locked in ADR-0008** §"GitHub-first-party SLSA over external Sigstore CI". Re-read the ADR if reconsidering.

### Action input details (from `actions/attest-build-provenance@v4.1.0` documentation)

Required inputs:
- `subject-name`: the registry-prefixed image reference *without* the digest suffix (e.g., `ghcr.io/<owner>/oh-my-bmad-base`). The action appends the digest automatically.
- `subject-digest`: the image's SHA256 content digest (e.g., `sha256:abc...`), as output by `docker/build-push-action`'s `digest` output.
- `push-to-registry: true`: tells the action to publish the attestation to the OCI registry alongside the image, instead of only making it queryable via the GitHub Attestations API.

The action handles OIDC token minting, Sigstore Fulcio cert request, signing, and registry upload internally. No additional config needed.

### Where the digest comes from in our workflow

Existing release.yml (lines 84–101 for base, lines 143–161 for services) already passes `id: build` to `docker/build-push-action`. The build action's output `digest` is documented stable: `${{ steps.build.outputs.digest }}` returns `sha256:abc123...` after a successful multi-arch push. **No extra configuration needed in the build step.**

### Image-reference construction

```yaml
# Base image
subject-name: ${{ env.REGISTRY }}/${{ env.OWNER }}/oh-my-bmad-base
subject-digest: ${{ steps.build.outputs.digest }}

# Matrix service
subject-name: ${{ env.REGISTRY }}/${{ env.OWNER }}/oh-my-bmad-${{ matrix.service }}
subject-digest: ${{ steps.build.outputs.digest }}
```

Note: `env.REGISTRY = ghcr.io` and `env.OWNER = ${{ github.repository_owner }}` are already defined at the workflow top level. No new env vars needed.

### Permissions delta

Current:

```yaml
permissions:
  contents: read
  packages: write
```

Target:

```yaml
permissions:
  contents: read
  packages: write
  id-token: write       # NEW — required for SLSA keyless OIDC + cosign keyless (Story 8.3)
  attestations: write   # NEW — required for push-to-registry: true (SLSA + cosign attest)
```

**Note:** the same two new permissions also unlock Story 8.3 (cosign keyless) and Story 8.4 (cosign attest). Adding them in Story 8.2 prep-stages the workflow for the remaining Epic 8 stories. No security regression — the permissions scope to this workflow only.

### Where to insert the steps

For consistency with Story 8.1's pattern, insert the SLSA attestation step **after the `:latest` tagging step**, sharing the section's commentary block. Suggested location:

- **Base job:** after `Tag base as :latest`, before the SBOM generation steps (so the SLSA attestation is a sibling supply-chain artifact to the SBOM rather than ordered through it).
- **Matrix job:** after `Tag <service> as :latest`, before the SBOM steps.

This ordering is operationally arbitrary (both attestations and SBOMs run on the same content digest), but visually groups supply-chain operations together.

### Architecture compliance

This story implements **FR54** (SLSA L2 attestation, see [`_bmad-output/planning-artifacts/prd.md`](../planning-artifacts/prd.md) §"Phase 2 Scope Extension"), is decomposed under **Epic 8** (see [`epics.md`](../planning-artifacts/epics.md) §"Epic 8 — Supply-chain hardening"), and complies with **ADR-0008** ([`docs/adr/0008-cosign-slsa-sbom.md`](../../docs/adr/0008-cosign-slsa-sbom.md), §"Decision" item 1).

The story preserves all Phase 1 + Phase 2 invariants:

- **FR26 single-writer** — no DB or event-log writes from this story.
- **MCP stdio-only** — no MCP changes.
- **P2-I1 read-only-subscriber rule** — workflow-only change; no Phase 2 service additions.
- **P2-I3 derived metrics, not parallel instrumentation** — no `services/*` changes.
- **No `anthropic` SDK in platform code** — unchanged.

### Project Structure Notes

- File touched: `.github/workflows/release.yml` (workflow change — per project-context.md Cat 6, requires CODEOWNERS-named human reviewer; operator is the reviewer for this solo project).
- No code in `services/*`, `packages/*`, or `mcp-servers/*` is modified.
- No new event types, no schema_version bump, no new migration.
- Net impact on workflow: 2 new steps (1 in base job, 1 in matrix job) + 2 permission additions; estimated +14 lines.

### Testing standards summary

Static-correctness verification (operator-side, before merge):

1. **YAML lint:** `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` returns no error.
2. **SHA-pin verification:** `grep -c "actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32" .github/workflows/release.yml` returns exactly `2`.
3. **Permissions verification:** `grep "id-token: write\|attestations: write" .github/workflows/release.yml` returns 2 matches under the top-level `permissions:` block.

Runtime verification (operator-side, post-merge, on first Phase 2 release):

1. Tag a prerelease (e.g., `v0.1.1-rc-slsa-test`) to trigger the workflow without moving `:latest`.
2. Confirm GitHub Actions run completes successfully for all 8 services + base.
3. Run `cosign verify-attestation --type slsaprovenance --certificate-identity-regexp "^https://github.com/<OWNER>/oh-my-bmad/\.github/workflows/release\.yml@refs/tags/v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9.-]+)?$" --certificate-oidc-issuer "https://token.actions.githubusercontent.com" ghcr.io/<OWNER>/oh-my-bmad-base@<digest>` and confirm the attestation traces back to the workflow run + commit SHA. Regexp is anchored on both ends per code-review pass-2 F1+F3.
4. Repeat for one service image (e.g., `oh-my-bmad-registry-api`).
5. On green: move sprint-status entry `8-2-slsa-l2-provenance-attestation` → `done`. The runtime verification is sufficient for closure.

If runtime verification is deferred to a real release (recommended — no need to burn a test tag if Story 8.3 / 8.4 / 8.5 will trigger the same release pipeline soon), document the deferral in the `verified_via:` list of this file's frontmatter post-implementation.

### References

- [Source: `_bmad-output/planning-artifacts/prd.md`#Phase 2 Scope Extension — γ Supply-chain hardening (Epic 8)] FR54 SLSA L2 provenance.
- [Source: `_bmad-output/planning-artifacts/architecture.md`#Phase 2 Architecture Extension — Supply-chain pipeline (γ — Epic 8)] release.yml additions schematic.
- [Source: `_bmad-output/planning-artifacts/epics.md`#Epic 8: Supply-chain hardening (γ)#Story 8.2] AC + scope.
- [Source: `docs/adr/0008-cosign-slsa-sbom.md`] supply-chain triumvirate decision.
- [Source: `_bmad-output/implementation-artifacts/8-1-sbom-anchore-sbom-action.md`] precedent for SHA-pin discipline + ADR/sprint-status promotion pattern.
- [Source: GitHub Actions docs — `actions/attest-build-provenance`] action input reference.
- [Source: SLSA framework — https://slsa.dev/spec/v1.0/levels#build-l2-hosted-build-platform] L2 definition.

## Dev Agent Record

### Agent Model Used

`Claude Opus 4.7 (1M context)` — invoked via `/bmad-dev-story 8.2`, 2026-05-15.

### Debug Log References

No debug log entries required. Implementation was deterministic — workflow YAML additions only, no runtime debugging needed.

### Completion Notes List

- **Static verification:** All AC-6 gates pass on the first attempt — YAML lint, SHA-pin grep (2 occurrences), step-name grep (2 occurrences), permissions grep (both `id-token: write` + `attestations: write` present under top-level block).
- **Insertion-point decision:** Steps placed after the `:latest` tagging step (per Dev Notes ordering) — visually groups SLSA + SBOM as sibling supply-chain operations after image push completion.
- **Permissions delta wisdom:** `id-token: write` + `attestations: write` were intentionally added in this story rather than deferring split adds to Stories 8.3/8.4 — they are joint prerequisites for Epic 8's remaining stories; landing them now removes a per-story edit and keeps the permission scope review-visible as a single block.
- **Action input correctness:** `subject-digest: ${{ steps.build.outputs.digest }}` was verified against the existing `docker/build-push-action@v6` (already pinned with `id: build`) — no need to add new `id:` references or expose new outputs.
- **No tests added.** Workflow changes don't have unit tests in this repo; runtime verification happens on the next release-tag push. Static-correctness (YAML lint + grep) is the in-PR gate.
- **Phase 1 invariant regression check:** No `services/*`, `packages/*`, `mcp-servers/*` code touched. Workflow-only change with zero blast radius on platform code. FR26 single-writer, MCP stdio, envelope immutability, NFR-M3 additive schema, no `anthropic` SDK in platform code — all unchanged.

### File List

Modified (1 file):

- `.github/workflows/release.yml` — added 2 SLSA attestation steps (one per job) + 2 new top-level permissions (`id-token: write`, `attestations: write`). Net +36 lines.

Story file (this file) updated to reflect implementation state (Tasks/Subtasks → all `[x]` except deferred runtime verification, Dev Agent Record populated, Status → `review`).

`sprint-status.yaml`: `8-2-slsa-l2-provenance-attestation` transitioned `ready-for-dev` → `in-progress` → `review`.

## Change Log

| Date | Change | By |
|---|---|---|
| 2026-05-15 | Created story spec with full dev context (`/bmad-create-story 8.2`) | R2d2 via Claude |
| 2026-05-15 | Implemented Story 8.2 — SLSA L2 attestation added to release.yml base + matrix jobs; permissions block extended; YAML lint + AC verification all green; Status → `review` | R2d2 via Claude (`/bmad-dev-story 8.2`) |

---

## AC-by-AC verification checklist (post-implementation)

| AC | Statement | Static evidence | Runtime evidence | Status |
|---|---|---|---|---|
| AC-1 | 8 images per release carry SLSA attestation | 2 SLSA steps in workflow (`grep -c "Generate SLSA L2 provenance attestation"` → 2); matrix lists 7 services; base job runs unconditionally; SLSA steps gated only by `if: steps.build.outputs.digest != ''` (loud-fail guard, not a release-blocker) | Pending next release-tag push | **static ✓ / runtime ⏳** |
| AC-2 | `cosign verify-attestation --type slsaprovenance` succeeds with anchored cert-identity-regexp | Workflow comment now carries the **anchored** regexp `^https://github.com/${OWNER}/oh-my-bmad/\.github/workflows/release\.yml@refs/tags/v[0-9].*` (fix F1) | Pending next release-tag push | **static ✓ / runtime ⏳** |
| AC-3 | `id-token: write` + `attestations: write` added minimally | Both present under top-level `permissions:` (lines 22-25); `contents: read` + `packages: write` unchanged; rationale block above the permissions documents per-permission necessity per [actions/attest-build-provenance permissions docs](https://github.com/actions/attest-build-provenance#workflow-permissions) (fix F14) | n/a | **✓** |
| AC-4 | SHA-pinned `a2bbfa25...  # v4.1.0` | 2 occurrences of the canonical SHA + version comment; SHA matches `gh api repos/actions/attest-build-provenance/git/ref/tags/v4.1.0 --jq '.object.sha'` (released 2026-02-26) | n/a | **✓** |
| AC-5 | Matrix per-service isolation preserved | `fail-fast: false` at line 179; SLSA step has no `continue-on-error`, no broader `if:` that would mask failure. **Reasoning chain (fix F13):** SLSA-step exception → job-step failure → GitHub Actions surfaces the job as failed → `fail-fast: false` prevents matrix-wide cancellation → sibling matrix entries continue independently | Pending observation during a deliberately-failing matrix run (low priority — static reasoning is airtight) | **static ✓** |
| AC-6 | YAML lint + actionlint clean | `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` → no error; `actionlint .github/workflows/release.yml` → exit 0 (fix F10); `yamllint` not installed in this env, substituted by `actionlint` which subsumes both YAML structure + Actions semantics | n/a | **✓** |
| AC-7 | Impl artifact + sprint-status promoted post-impl | Frontmatter `status: review` (fix F9); explicit AC checklist (this section, fix F9); Tasks/Subtasks all `[x]` except deferred runtime verification; sprint-status.yaml `8-2-...: review`; Senior Developer Review section added (this revision) | n/a | **✓** |

## Done-gate checklist (must all be checked before status → done)

- [x] All 7 AC: static evidence captured (above).
- [ ] **AC-1 runtime verification:** prerelease tag pushed (e.g., `v0.1.x-rc-slsa-test`); workflow run completes successfully; SLSA attestation present in GHCR for at least the base image + 1 service.
- [ ] **AC-2 runtime verification:** `cosign verify-attestation --type slsaprovenance` succeeds against the prerelease image's digest with the anchored cert-identity-regexp.
- [ ] **AC-5 (optional) runtime verification:** observe one matrix entry failing while siblings continue (low priority; static reasoning sufficient).
- [ ] `verified_via:` frontmatter list populated with the release URL + cosign verify-attestation output excerpt.
- [ ] `sprint-status.yaml` entry transitioned `review` → `done`.
- [ ] ADR-0008 status remains `accepted` (Story 8.1 already promoted it; no re-promotion needed).

**Closure trigger:** the next release-tag push (could be a deliberate prerelease, or — more efficiently — the natural release after Stories 8.3 + 8.4 also land, since one release run validates all three at once).

## Senior Developer Review (AI)

**Reviewers:** 3 parallel adversarial lanes — Blind Hunter, Edge Case Hunter, Acceptance Auditor (via `bmad-code-review` workflow).
**Review date:** 2026-05-15.
**Diff source:** commit `3c7b9af`.
**Outcome:** REQUEST CHANGES → all 14 findings addressed in follow-up commit.

### Findings + Resolution

| ID | Sev | Reviewer | Finding | Resolution |
|---|---|---|---|---|
| F1 | HIGH | Blind Hunter | Verify-command regexp in workflow comment unanchored (`.*/oh-my-bmad/...`) — fork-attestation spoofing risk | Anchored to `^https://github.com/${OWNER}/oh-my-bmad/\.github/workflows/release\.yml@refs/tags/v[0-9].*` in workflow comment + cross-referenced docs/deployment-guide.md as canonical location |
| F2 | HIGH | Acc. Auditor + Blind Hunter | AC-2 runtime verification deferred without explicit `done`-gate | Added explicit "Done-gate checklist" section (above) with runtime-verification items as unchecked subtasks; story remains `review`, not `done`, until those check off |
| F3 | HIGH | Edge Case Hunter | SLSA step runs unconditionally on prerelease tags — orphan attestations | **Policy decision documented in workflow comment + ADR-0008:** SLSA attestation runs for ALL released tags (stable + prerelease) deliberately. Operators verify staging-image pulls too. Rekor growth is expected behavior. |
| F4 | HIGH | Edge Case Hunter + Blind Hunter | `timeout-minutes` budget may collapse on cold cache + Stories 8.3/8.4 additions | Base job: 25 → 30 (5 min margin). Services job: 30 → 40 (10 min margin). Comments updated with budget breakdown. |
| F5 | MEDIUM | Blind Hunter | Permissions pre-staged for Stories 8.3/8.4 — could be removed by future reviewer as "unused" | Comment block above `permissions:` documents intent + names the future stories that consume each permission |
| F6 | MEDIUM | Edge Case Hunter | No empty-digest guard before SLSA step | Added `if: steps.build.outputs.digest != ''` to both SLSA steps; fails loudly with clear surface vs silent attestation failure |
| F7 | MEDIUM | Edge Case Hunter | Tag-retry produces duplicate Rekor entries (cosmetic) | Documented in ADR-0008 §"Operational policy notes" with mitigation (use `gh run rerun` only when build step failed) |
| F8 | MEDIUM | Edge Case Hunter | Sigstore Fulcio/Rekor outage = hard release block; no stated policy | Documented in ADR-0008 §"Operational policy notes" as **intentional** with cost/benefit analysis; operators monitor https://status.sigstore.dev during stuck releases |
| F9 | MEDIUM | Acc. Auditor | Stale frontmatter `status: ready-for-dev`; no AC checklist | Frontmatter updated to `status: review` (this revision); explicit AC-by-AC checklist added (above) |
| F10 | MEDIUM | Acc. Auditor | `actionlint`/`yamllint` not cited as run | `actionlint .github/workflows/release.yml` → exit 0 captured in AC-6 row above; yamllint not installed, actionlint substitutes (subsumes both YAML structure + Actions semantics) |
| F11 | LOW | Blind Hunter | SLSA step has no `id:` for future chaining | `id: attest-base` + `id: attest-service` added to both SLSA steps; cheap forward-compat for Stories 8.3/8.4 |
| F12 | LOW | Edge Case Hunter | SLSA/SBOM failure asymmetry undocumented | Documented in ADR-0008 §"Operational policy notes" with **8-terminal-state** table (2³ combinations of signature × SLSA × SBOM-attest binary outcomes) + deploy-side response per state |
| F13 | LOW | Acc. Auditor | AC-5 "verified by YAML lint" is weak evidence | Explicit reasoning chain (SLSA-step exception → job-step failure → matrix isolation via `fail-fast: false`) captured in AC-5 row above |
| F14 | LOW | Acc. Auditor | AC-3 "minimal permissions" claim not cited from official docs | Added [actions/attest-build-provenance permissions docs](https://github.com/actions/attest-build-provenance#workflow-permissions) link in AC-3 row above |

### Positive observations (preserved from Blind Hunter)

- SHA-pinning discipline consistent + canonical against GitHub API.
- `steps.build.outputs.digest` is correctly the manifest-index digest for multi-arch images (action handles both amd64 + arm64 in one attestation per documented v4.x behavior).
- `fail-fast: false` matrix preserves sibling isolation — static reasoning airtight.
- Top-level `permissions:` block is the right granularity (both jobs legitimately need both permissions).
- `imagetools create :latest` before SLSA step is safe (GHCR tag-copy preserves manifest digest).
- `ci.yml` + `nightly.yml` zero blast radius from new permissions.

### Review Follow-ups (AI) — all resolved

- [x] [AI-Review HIGH] F1 — Anchor verify-regexp in workflow comment
- [x] [AI-Review HIGH] F2 — Explicit done-gate checklist
- [x] [AI-Review HIGH] F3 — Document prerelease attestation policy
- [x] [AI-Review HIGH] F4 — Bump timeout-minutes
- [x] [AI-Review MED] F5 — Document permissions pre-staging
- [x] [AI-Review MED] F6 — Empty-digest guard
- [x] [AI-Review MED] F7 — Document tag-retry policy in ADR-0008
- [x] [AI-Review MED] F8 — Document Sigstore-outage policy in ADR-0008
- [x] [AI-Review MED] F9 — Update frontmatter + add AC checklist
- [x] [AI-Review MED] F10 — Cite actionlint evidence
- [x] [AI-Review LOW] F11 — Add `id:` to SLSA steps
- [x] [AI-Review LOW] F12 — Document 4 terminal states in ADR-0008
- [x] [AI-Review LOW] F13 — AC-5 reasoning chain
- [x] [AI-Review LOW] F14 — AC-3 docs citation

---

## Senior Developer Review (AI) — Pass-2 (2026-05-15)

User requested a second-opinion adversarial review on Story 8.2 via `/bmad-code-review 8.2`. Three reviewer lanes (Blind Hunter / Security Edge-Case / Acceptance Auditor) ran in parallel and surfaced **14 NEW findings** the first pass missed — including 3 HIGH-severity issues where the first review's resolutions were structurally incomplete. All 14 fixes applied in this same dev pass per user instruction "fix all issues even minors" (carry-over from the first review's policy).

### Pass-2 Action Items

| ID | Sev | Title | Resolution |
|---|---|---|---|
| F1 | **H** | AC-2 + Testing-Standards in spec body retained the unanchored regexp; first-pass F1 fix touched only workflow comments + justfile + deployment-guide.md | [x] Spec body AC-2 (line 17) AND Testing Standards section step 3 (line 158) updated to the anchored form `^...$` with semver tail; cross-referenced |
| F2 | **H** | `if: digest != ''` SKIPS step, doesn't FAIL — image could land in GHCR unattested with green CI | [x] Added explicit `Validate build digest before attestation` step in BOTH base and matrix jobs; `[ -z DIGEST ] && exit 1` is the primary contract; `if:` retained as defense-in-depth |
| F3 | **H** | Cert-identity regexp lacks `$` end anchor — `v[0-9].*` matches arbitrary suffix | [x] Regex tightened to `^...@refs/tags/v[0-9]+\.[0-9]+\.[0-9]+(-[A-Za-z0-9.-]+)?$` in workflow comments, justfile, spec, and architecture.md (4 sites) |
| F4 | M | `OMB_GHCR_OWNER` operator-rebindable — F1 trust anchor mutable via .env | [x] justfile warns when `OMB_GHCR_OWNER ≠ canonical owner` (suppressible via `OMB_ACK_CUSTOM_OWNER=1`); hard-fails on malformed value; threat model documented in ADR-0008 §"Known limitations" |
| F5 | M | SBOM (Anchore) generation step lacked digest-non-empty guard | [x] Added `if: steps.build.outputs.digest != ''` to both SBOM generation steps (base + matrix) for consistency with SLSA + cosign steps |
| F6 | M | Digest format `sha256:[a-f0-9]{64}` not validated (`if != ''` accepts `sha256:` prefix-only) | [x] `Validate build digest` step uses `grep -qE '^sha256:[a-f0-9]{64}$'`; justfile applies same regex to .env-supplied digests; both fail fast with clear error |
| F7 | M | Workflow-level `id-token: write` silently inherits to future jobs | [x] Added explicit WARNING block + 3-step audit checklist above the `permissions:` block; new jobs MUST override with `permissions: {}` if they don't need OIDC |
| F8 | M | architecture.md still used unanchored regexp + listed `metrics-subscriber` instead of shipped service set | [x] Updated arch.md lines 1374, 1379 to anchored regexp; corrected service list to shipped 8-entry array (base + 7 services including console-cli) |
| F9 | M | AC-5 base-job failure-isolation reasoning unanalyzed (only services matrix covered by `fail-fast: false`) | [x] **Note added below:** base job is singleton; its SLSA failure does NOT prevent services job from running (`needs:` checks job-ran, not all-steps-passed). The new `Validate build digest before attestation` step fails the base job loudly, which cascades to `needs: build-and-push-base` correctly. This is now the primary contract. |
| F10 | L | TOCTOU push→attest not documented in ADR (mitigation: digest-binding) | [x] Added bullet to ADR-0008 §"Known limitations" explicitly stating attestation binds to digest (not mutable tag); tag-immutability is defense-in-depth, not primary defense |
| F11 | L | Stale comments — `provenance: false` future tense + "8.3/8.4 future" markers | [x] Updated `provenance: false` comment block (line 109-118) to explicit "SLSA via attest-build-provenance / SBOM via sbom-action / signing via cosign-installer — all shipped"; top-of-file permissions comment now annotates each story with `[shipped <SHA>]` |
| F12 | L | Story 8.2 F12 line said "4-terminal-state" but ADR actually has 8 (2³ combinations) | [x] Updated F12 row in the prior triage table to **8-terminal-state** with explicit 2³ explanation |
| F13 | L | `verify-images` tempfile leak on SIGINT + SLSA multi-arch undocumented | [x] Added `trap cleanup_tmpfiles EXIT INT TERM` + array-tracked tempfiles in justfile; ADR-0008 §"Known limitations" gained an explicit bullet that SLSA IS multi-arch (manifest-index-bound), unlike SBOM which is amd64-only |
| F14 | L | Hygiene cluster — `verified_via:` no SHAs, version-comment precision, AC-7 code-touch diff missing | [x] `verified_via:` updated to reference pass-2 commit + 14-finding triage; major comments around `provenance: false` and permissions block now explicitly cross-reference shipped stories with SHAs |

### Base-job failure-isolation (F9 detailed analysis)

The AC-5 reasoning chain was originally written for the services matrix only. For completeness:

| Scenario | Behavior |
|---|---|
| Base job step `build` fails | `build-and-push-base` fails → `needs: build-and-push-base` blocks services matrix entirely. ✓ Correct cascade. |
| Base job step `attest-base` fails | `build-and-push-base` fails → `needs:` blocks services matrix. ✓ Correct cascade (post-F2 fix; pre-fix: `if:` would SKIP, base job would show green, services matrix would run against an unattested base image — the **exact race the F2 fix eliminates**). |
| Base job step `validate-digest-base` fails (new) | Fails the base job immediately with clear error. ✓ Primary contract. |
| Service matrix entry step `validate-digest-service` fails | Fails that matrix entry only (`fail-fast: false`). ✓ Correct isolation. |
| Service matrix entry step `attest-service` fails | Fails that matrix entry only. ✓ Correct isolation (pre-fix had the same `if:` SKIP risk as the base job; F2 fix applied to both). |

### Pass-2 verdict

**APPROVED with all 14 findings resolved**. The first-pass review had 3 high-severity gaps (F1 incomplete, F2 wrong semantics, F3 unanchored end) that would have allowed unattested images into GHCR under realistic failure modes. The F2 fix (explicit `Validate build digest` step) is the most consequential — it converts the SLSA contract from "skip silently on empty digest" to "fail the job loudly", which is what the original `if:` guard's name had suggested but never delivered.

Total fixes applied: **14** (3 H + 6 M + 5 L). Story 8.2 remains in `review` status; awaiting joint Epic-8 runtime closure on next release tag along with Stories 8.3-8.6.

---

## Frontmatter

```yaml
---
story_id: 8-2-slsa-l2-provenance-attestation
epic: 8
phase: 2
fr: FR54
nfr: NFR-S9
status: review
implemented_by: R2d2
date: 2026-05-15
reviewed_at: 2026-05-15
review_outcome: changes_requested_then_resolved
scope_delta: null
deferred_items: []
sha_pins:
  actions/attest-build-provenance: a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32  # v4.1.0 published 2026-02-26
verified_via:
  - YAML lint (python3 yaml.safe_load) — release.yml parses cleanly
  - actionlint .github/workflows/release.yml — exit 0
  - GitHub API SHA resolution — pin traceable to canonical release tag
  - Pass-1 review (2026-05-15 morning) — 14 findings (3H + 6M + 5L), all resolved at commit df52455
  - Pass-2 review (2026-05-15 afternoon) — 14 NEW findings (3H + 6M + 5L), all resolved in this dev pass
  - (pending) runtime verification on next release-tag push (will validate end-to-end with shipping Stories 8.3-8.6)
---
```
